export default schema;
declare namespace schema {
    const type: string;
    const elements: any[];
    namespace options {
        const variant: string;
        const showNavButtons: boolean;
    }
}
